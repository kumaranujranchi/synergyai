# Email Configuration for Contact Form
# IMPORTANT: Never commit real credentials to Git!
# Set these in your deployment environment (Netlify, Vercel, etc.)
EMAIL_USER=your-email@domain.com
EMAIL_PASS=your-app-specific-password

# Environment
NODE_ENV=production

# Figma Integration Configuration
# Get your access token from: https://www.figma.com/settings (Personal Access Tokens)
FIGMA_ACCESS_TOKEN=your_figma_access_token_here
# FIGMA_TEAM_ID=your_team_id_here
# FIGMA_PROJECT_ID=your_project_id_here

# Security Note:
# - Use App Passwords for Gmail (not your regular password)
# - Set these as environment variables in your hosting platform
# - Never include real credentials in your code or .env files that get committed
# - Keep your Figma access token secure and rotate it regularly
